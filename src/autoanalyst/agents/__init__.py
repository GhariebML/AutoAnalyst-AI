"""AutoAnalyst AI — Agentic layer package.

This package contains the LangGraph-based multi-agent system that
orchestrates the end-to-end data analysis workflow.  Each sub-module
implements a single specialised agent node.

Team 5 (Machine Learning Agent) contributes:
  - modeling_tools.py   — LangChain-compatible tool wrappers
  - model_decider.py    — autonomous task / algorithm selection logic
  - modeling_agent.py   — LangGraph node that connects everything
"""
