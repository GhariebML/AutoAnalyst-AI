# AutoAnalyst AI — LLM narration (all optional; system runs fully without it)
# Copy to .env and fill in only what you use. NEVER commit real keys.

AUTOANALYST_LLM_ENABLED=false
AUTOANALYST_LLM_PROVIDER=openai
AUTOANALYST_LLM_MODEL=gpt-4o-mini

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
